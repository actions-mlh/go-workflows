Code Generation

issues: 
1) remove additionalProperites? 
2) change the public schema? /definitions/eventObject

JSON schema keyword Order:
1) Create Parent + Child + Node
    a) oneOf, allOf, anyOf 
    b) Properties (objects), Items (array), scalar types (float64, string, int, null(nil), etc...)
    c) $ref 
        * oneOf -> properties -> types

2) UnmarshalYAML (code buffer)
    a) create unmarshal for oneOf, allOf, anyOf, (yaml.Kinds)
    b) check if required key exists after recursive step -> (value.Decode(&node.Value))
        * i.e, if on: <nil>, we return fmt.Error(line:col) 

3) Naming Value struct
    a) anything other than allOf, anyOf, oneOf -> Parent + Child + Value
    b) enum -> Constants
    c) const -> Constant
    d) oneOf -> Parent + Child + OneOf

Package 
solutions:
    1) Write it out since we are using one directory
    2) Read -i (input path) from user, (package %s, input path)

Imports:
solutions:
    1) Manually write it out
    2) Create a buffer and slice, as we are reading the json Schema
    we can scan the imports necessary and append to slice

